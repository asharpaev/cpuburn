node {
    label 'slave'
    stage('Checkout'){
        checkout scm
        sh "ip addr"
    }
    stage('Build') {
        sh "ip addr"
        sh "ip addr"
        sh "docker build --no-cache"
    }

}